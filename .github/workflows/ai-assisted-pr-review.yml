name: AI-Assisted PR Security Review

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  ai-assisted-pr-review:
    name: Run AI-Assisted PR Security Review
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed files
        run: |
          mkdir -p reports

          if [ "${{ github.event_name }}" = "pull_request" ]; then
            git fetch origin "${{ github.base_ref }}"
            git diff --name-only "origin/${{ github.base_ref }}...HEAD" > reports/changed-files-all.txt
          else
            find fixtures ai-reviewer docs .github policy dashboard monitoring -type f > reports/changed-files-all.txt
          fi

          echo "All changed files:"
          cat reports/changed-files-all.txt

          > reports/changed-files.txt

          while IFS= read -r file; do
            if [ -f "$file" ]; then
              echo "$file" | grep -E '\.(py|yml|yaml|json|Dockerfile|dockerfile|tf|sh|bash|js|ts|go|java|cs|rb|php)$|(^|/)Dockerfile$' >> reports/changed-files.txt || true
            else
              echo "Skipping deleted or missing file: $file"
            fi
          done < reports/changed-files-all.txt

          echo ""
          echo "Security-relevant existing files selected for review:"
          cat reports/changed-files.txt || true

      - name: Run AI-assisted security review on changed files
        run: |
          FILES=$(cat reports/changed-files.txt | tr '\n' ' ')

          if [ -z "$FILES" ]; then
            python3 - <<'PY'
            from pathlib import Path

            Path("reports").mkdir(exist_ok=True)
            Path("reports/ai-assisted-security-review.md").write_text(
                "# AI-Assisted Security Review\n\n"
                "Decision: PASS\n\n"
                "No security-relevant changed files detected. Documentation, images, and non-executable assets were skipped.\n"
            )
            PY
          else
            python3 ai-reviewer/review_changed_files.py \
              --paths $FILES \
              --output reports/ai-assisted-security-review.md
          fi

          cat reports/ai-assisted-security-review.md

      - name: Run finding triage assistant
        run: |
          python3 ai-reviewer/finding_triage_assistant.py

      - name: Create PR security review comment
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          python3 - <<'PY'
          import json
          import os
          import subprocess
          from pathlib import Path

          report_path = Path("reports/ai-assisted-security-review.md")
          changed_path = Path("reports/changed-files.txt")
          all_changed_path = Path("reports/changed-files-all.txt")

          report = report_path.read_text() if report_path.exists() else "No report generated."
          changed = changed_path.read_text() if changed_path.exists() else "No security-relevant changed files captured."
          all_changed = all_changed_path.read_text() if all_changed_path.exists() else "No changed files captured."

          body = (
              "## AI-Assisted PR Security Review\n\n"
              "This is an advisory security review generated by the Secure SDLC workflow.\n\n"
              "### All changed files\n\n"
              "<pre>\n"
              f"{all_changed}"
              "</pre>\n\n"
              "### Security-relevant files reviewed\n\n"
              "<pre>\n"
              f"{changed}"
              "</pre>\n\n"
              "### Review summary\n\n"
              f"{report}\n\n"
              "### Human approval note\n\n"
              "This review is advisory. High-risk findings require human validation before merge.\n"
          )

          payload = json.dumps({"body": body})
          repo = os.environ["REPO"]
          pr_number = os.environ["PR_NUMBER"]
          url = f"https://api.github.com/repos/{repo}/issues/{pr_number}/comments"

          subprocess.run(
              [
                  "curl",
                  "-sS",
                  "-X",
                  "POST",
                  "-H",
                  "Accept: application/vnd.github+json",
                  "-H",
                  f"Authorization: Bearer {os.environ['GH_TOKEN']}",
                  "-H",
                  "X-GitHub-Api-Version: 2022-11-28",
                  url,
                  "-d",
                  payload,
              ],
              check=True,
          )
          PY

      - name: Upload PR review reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ai-assisted-pr-security-review
          path: |
            reports/changed-files-all.txt
            reports/changed-files.txt
            reports/ai-assisted-security-review.md
            reports/finding-triage-report.md

      - name: Enforce advisory security gate
        run: |
          if grep -q "Decision: BLOCK RECOMMENDED" reports/ai-assisted-security-review.md; then
            echo "AI-assisted security gate failed: BLOCK RECOMMENDED"
            exit 1
          fi

          echo "AI-assisted security gate passed."
